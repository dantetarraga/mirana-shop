import { MediaLibraryClient } from '@/features/uploads/components/MediaLibraryClient'

export const metadata = { title: 'Imágenes — Admin Mirana' }

export default function AdminMediaPage() {
  return <MediaLibraryClient />
}
